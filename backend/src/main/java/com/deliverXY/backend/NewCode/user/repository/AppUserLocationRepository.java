package com.deliverXY.backend.NewCode.user.repository;

import com.deliverXY.backend.NewCode.user.domain.AppUserLocation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AppUserLocationRepository extends JpaRepository<AppUserLocation, Long> {
}
